/**
 * Ambient atmosphere veil — soft gradient, no photo.
 */
export function SiteBackdrop() {
  return (
    <div className="site-backdrop" aria-hidden>
      <div className="site-backdrop__veil" />
    </div>
  )
}
